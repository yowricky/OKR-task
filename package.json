{
  "name": "task-app",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev:server": "pnpm --filter @app/server dev",
    "dev:desktop": "pnpm --filter @app/desktop tauri dev",
    "build:shared": "pnpm --filter @app/shared build",
    "test": "pnpm -r test"
  }
}
